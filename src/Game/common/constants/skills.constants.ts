import type { SkillId } from "@/common/enum/skill-ids";

export const SKILL_MAX_SEGMENTS = 3;

export const SKILL_SEGMENT_REGEN_MS = 15000;

export const SKILL_COSTS: Record<SkillId, number> = {
  blizzard: 2,
};

