/**
 * Skill IDs used for progression and loadout.
 * "blizzard" = Nevasca (starting skill).
 */
export type SkillId = "blizzard";

export const DEFAULT_SKILL: SkillId = "blizzard";

/** All known skill IDs in display order (for SkillsTab). */
export const SKILL_IDS_ORDER: SkillId[] = ["blizzard"];
